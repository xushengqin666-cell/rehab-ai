# 康复AI · 火柴人姿势分析（Web 版 v2.2 · 中英双语）

手机 + 电脑通用的响应式 Web App：浏览器实时摄像头 → AI 姿态识别 → **火柴人骨架**
动作分析，集成记录打卡、自我评估、理疗预约、**自定义动作**、**数据采集训练闭环**。
视频不上传，AI 与数据全在本地运行。**中英双语，自动检测浏览器语言，一键切换。**

## 🌍 线上版（直接发给别人用）
**https://xushengqin666-cell.github.io/rehab-ai/**
（手机浏览器打开即可用摄像头；桌面二维码：`康复AI-线上网址二维码.png`）

## 支持的动作（火柴人实时分析 + 自动计数）
| 动作 | 分析指标 | 数据标签（采集用） |
|------|---------|-------------------|
| 🦵 深蹲 | 膝盖角 / 前倾 / 深度 / **膝盖内扣** | good/shallow/deep/lean/valgus |
| 🚶 弓步蹲 | 前膝角 / 后膝角 / 前倾 | good/frontShallow/frontDeep/lean |
| 💪 俯卧撑 | 肘角 / 身体直线度 | good/shallow/sag |
| ⭐ **自定义** | 自己定角度、合格范围、计数规则、提示语 | good/bad |

## 快速开始（本机）
```bash
python serve.py
```
浏览器打开 `http://127.0.0.1:8000`（电脑可开摄像头；手机同 Wi-Fi 可访问，摄像头需 HTTPS）

## 手机实时摄像头：部署上线（免费 HTTPS）
- **已部署**：https://xushengqin666-cell.github.io/rehab-ai/（GitHub Pages，推送即自动更新）
- **方式A（1分钟）**：打开 https://app.netlify.com/drop → 拖入本文件夹 → 得到网址，手机电脑同网址访问
- **方式B**：双击 `deploy.bat` → 按提示用 GitHub Desktop 发布到 GitHub Pages
- 📱💻 **端手互通（二维码同步）**：设置页 → 一台点「显示二维码」、另一台点「扫描二维码」，
  训练记录/评估/预约/自定义动作几秒内自动合并（无需账号和服务器，数据压缩后本地二维码传输）
- 完整备份（含采集数据）：设置页「备份 / 恢复」→ 导出 JSON 文件 → 另一台「导入数据」

## 真实数据训练闭环（科研项目核心）
1. 训练页点「📝 数据采集」→ 选动作 → 摆姿势 → 点标签（每类 ≥50 条）
2. 设置页「导出采集 CSV」
3. 电脑上运行（Python 侧，rehab-ai 文件夹）：
   ```bash
   python train_from_web.py 康复AI采集数据-日期.csv
   ```
   → 按动作自动训练 RandomForest，输出交叉验证准确率 + `model_<动作>.pkl`

## 开发自测
浏览器打开 `index.html#selftest` → 控制台输出 9 项引擎自测（角度数学/内扣/计数含防抖/
弓步蹲/俯卧撑/自定义规则），全部 PASS 说明引擎正常。可用于 NCEA 测试证据。

更完整的 QA 自动化（双语回归 / 完整系统 / 云同步 / 二维码往返）在 `tests/` 目录，
用无头 Chrome + CDP 一键运行，见 `tests/README.md`。

## 目录结构
| 文件 | 说明 |
|------|------|
| index.html | 单页应用（训练/记录/评估/日程/设置） |
| style.css | 移动端优先响应式样式 |
| app.js | 主逻辑：火柴人绘制、采集、记录、评估、日程、自定义管理 |
| analysis.js | **动作引擎**：角度数学 + 深蹲/弓步蹲/俯卧撑/自定义分析 |
| i18n.js | **中英双语词典** + 语言切换 + 浏览器语言自动检测 |
| pose_landmarker_full.task | AI 姿态模型（本地，不上传） |
| vision_bundle.mjs + wasm/ | MediaPipe 浏览器运行时（本地，离线可用） |
| vendor/ | 二维码编解码库（jsQR / qrcode-generator，MIT，本地运行） |
| serve.py | 局域网启动器（打印手机访问地址） |
| deploy.bat | 一键部署准备（git 提交 + 上线说明） |
| netlify.toml | Netlify 部署配置 |

## 技术说明
- AI：MediaPipe PoseLandmarker（WebAssembly，本地运行）
- 判定阈值与 Python 版 rehab-ai 一致，可在 analysis.js 顶部调整
- 数据：浏览器 localStorage；导出 JSON 备份 / CSV 训练数据

## 更新日志
### v2.5（完整系统：资料 · 计划 · 提醒 · 报表 · 成就 · 云同步）
- 👤 **个人资料**：昵称 / 康复目标 / 伤情备注（仅存本机）
- 📋 **康复计划**：给每周的每天安排动作和目标次数，「今日任务」一键打卡并显示完成进度
- ⏰ **训练提醒**：每天定时提醒完成今日计划（浏览器通知）
- 📈 **统计报表**：30 天趋势图（每日次数 / 动作质量）、各动作分布
- 🏅 **成就系统**：9 枚徽章（初次训练 / 1000 次动作 / 连续打卡 / 计划达人 / 数据收集者…）
- ☁️ **云同步（账号系统）**：Supabase 登录注册 + 数据云端保存，换设备登录即恢复
  （设置页填入你的 Supabase URL 和 anon key 即可启用；二维码同步仍保留，无需账号）
- 📱💻 以上数据全部纳入二维码同步 / 云同步范围（计划、打卡、资料都会同步）

## 更新日志
### v2.17.1（体验打磨）
- 🪑 坐姿检查不再被「脚踝没照到」误报（脚在桌下照常分析）
- 📷 图片分析也能自动识别坐姿/站姿（按静止体态分类）
- ⏱ 保持计时只累计合格时间（姿势崩了暂停，不再扣秒）
- 🤖 AI 体检错误只统计近 7 天（老问题不永久扣分）
- 📱 iPhone 添加到主屏幕支持（apple-touch-icon）
- 🏪 商店文案同步更新为 9 个动作
### v2.17（智能识别大升级 + 体态检查）
- 🎯 **识别更准**：静止/运动双层判定（髋部 1.5 秒运动历史）——深蹲底部保持不再误判、弯腰拿东西不再串成深蹲，平板支撑也认得出
- 🪑 **坐姿检查**：久坐学习/办公时自查——驼背、头前伸、蜷腿实时提醒（侧面照最准）
- 🧍 **站姿检查**：躯干前倾、头前伸、高低肩实时提醒
- ⏱ 体态动作按「保持 30 秒计 1 次」计数，姿势崩了扣 3 秒
- 📋 9 个内置动作全部纳入康复计划
### v2.16.1（全面自检修复）
- 🐛 修复：从未训练过的新用户也能收到鼓励建议（此前只扣分不提示）
- 🔤 修复：反馈报告标签文案、英文模式自测详情国际化
- ✅ 自检强化：翻译覆盖率 100% 校验脚本、更新卡片 UI 模拟测试（55 项全过）
### v2.16（AI 系统管家 + 真·自主更新）
- 🤖 **AI 系统管家**：启动自动体检（健康评分 0-100），根据训练习惯/系统异常/版本状态给出智能建议
- 💬 **自动收集反馈**：AI 自动附带系统体检报告，一键提交到 GitHub（绝不包含视频/姓名/邮箱）
- 🔄 **更新方式可选**：全自动（安卓静默下载、空闲自动装）/ 提示我 / 关闭；更新卡片显示版本说明
- 🛡 AI 全程本地运行（规则引擎，离线可用），隐私政策同步更新
### v2.15（自主更新）
- 🔄 **自主更新**：启动自动检查新版本——网页版空闲时静默下载并自动重启；
  安卓 App 一键下载安装包并唤起系统安装器（走国内可访问的 CDN）
- 🔍 设置页新增「检查更新」按钮，手动检查随时更新
### v2.14（智能动作识别）
- ✨ **自动识别动作**：无需手动选动作，AI 自动判断你正在做深蹲/弓步/俯卧撑/
  椅子起坐/搬重物/上台阶/肩上举，随时自动切换分析标准
- 🛡 沿用同一套统一标准与受伤风险警报，自动切换不打断计数
### v2.13（首次引导 + 自动更新检测 + 商店上架包）
- 👋 **首次启动引导**：4 步介绍核心功能（AI 分析/计划/报表/账号），可跳过，看过不再出现
- 🔄 **自动更新检测**：每天检查一次 GitHub 最新版本，发现新版在训练页提示下载
- 🏪 **应用商店上架准备完成**：Play 专用 AAB（已签名）、隐私政策页、
  商店文案/数据安全答案/截图/推广图全部就绪（桌面「安装包」文件夹 + 上架指南）
### v2.12（语音教练 + 安卓 APK）
- 🎙 **语音播报**（系统 TTS，离线可用）：危险警报语音提醒、风险要领播报、
  每 5 次计数里程碑播报、目标达成播报；设置页可开关
- 📱 **安卓 APK 打包**（Capacitor）：真正可安装到手机的 App，图标/启动即登录页/
  全部功能与网页版一致，完全离线可用
### v2.11（本地账号系统：现在就能注册登录）
- 🔐 **真正的账号系统立即可用**：打开 App 即登录页 → 注册/登录（密码 PBKDF2 10 万次
  加密存本机）→ 每位用户的训练/计划/预约/成就数据**按账号隔离**，退出再登录数据都在
- 🔐 首次注册时自动把本机原有数据迁移进账号；访客模式可选跳过
- ☁️ 配置 Supabase 后：注册/登录会**自动同步开通云端账号**（同邮箱密码），
  数据变更 4 秒内自动上传、换设备登录自动拉回——本地与云端无缝合一
- 🚪 退出登录 → 回到登录页；验收测试扩展至 **43 项全绿**
### v2.10（真实 App 式账号系统）
- 🔐 **启动即登录页**：未登录时打开 App 直接看到登录/注册界面（Logo + 邮箱密码 +
  跳过访客模式），登录成功直接进入主界面——和真实 App 一模一样
- ⚙️ 服务器配置整合进登录页（首次粘贴 Supabase URL + anon key，之后永久生效）
- 👤 设置页顶部新增**账号卡片**（头像字母 + 邮箱 + 立即同步 + 退出登录）
- 🔄 **自动云同步**：登录后保存训练/计划/预约/评估等数据都会 4 秒内自动上传云端，
  换设备登录自动恢复（无需手动点同步）
- 🚪 退出登录 → 回到登录页；验收测试扩展至 **40 项全绿**
### v2.9（上市级完善 + 全系统验收）
- ✅ **36 项全功能验收测试**（`tests/full.mjs`）：7 动作引擎、摄像头全流程、计划闭环、
  成就 9/9 矩阵、评估、预约、自定义动作增改删、备份导入导出、云同步端到端、
  二维码往返、警报 UI、PWA、微信提示、双语切换 —— 全部通过、零控制台错误
- 📄 新增**隐私政策**与**免责声明**（健康类 App 上市必备）
- 🏷 设置页显示**版本号**；新增**分享 App**按钮（系统分享/复制链接）
- 🚨 新增警报 UI 自检入口（`?alarmtest=1`）
### v2.8（日常高频动作 + 统一标准 + 受伤风险警报）
- 🆕 **新增 4 个日常最高频动作**：椅子起坐、搬重物·髋铰链、上台阶、肩上举（共 7 个内置动作）
- 📏 **统一标准**：每个动作都有明确的「合格角度标准」，训练页动作说明下方实时展示
- ⚠️ **受伤风险三级提醒**：正常 → 黄色提醒（膝内扣/前倾/代偿）→ 🚨 **红色警报**
  （声音 + 手机震动 + 红光闪烁），针对弓背搬物、严重膝内扣、过度前倾等危险姿势
- 📊 训练记录新增「危险帧 %」，危险动作占比一目了然
- 🧪 引擎自测扩展到 14 项（含新动作引擎 + 风险警报触发）
### v2.7（闭环体验 + QA 体系）
- 🎯 **训练↔计划闭环**：训练页显示「今日目标」进度条；训练达标后**自动打卡**今日计划并提示
- 📉 评估页新增「评估分数趋势」曲线（最近 10 次）
- 🔄 App 自动检测新版本并提示刷新（PWA Service Worker）
- ☁️ 采集数据（科研 CSV 数据）纳入云同步范围（二维码同步保持轻量不携带）
- 🧪 `tests/` 目录：双语回归 / 完整系统 / 云同步 / 二维码往返 全套 CDP 自动化测试
  （含模拟 Supabase 服务器），发布前一键验证
### v2.6（PWA + 云同步验证）
- 📲 **PWA 可安装**：`manifest.json` + Service Worker + 应用图标（火柴人徽标），
  手机浏览器「添加到主屏幕」后像原生 App 一样全屏使用；离线也能打开
- 🚀 离线策略：页面/JS 网络优先（更新立即生效），模型/wasm 大文件缓存优先（省流量）
- ☁️ 云同步通过**端到端验证**（模拟 Supabase：注册/登录/推送/换设备恢复/增量合并全链路）
- 🐛 修复：云同步注册参数 bug、upsert 冲突参数、分享卡片（OG）元信息
### v2.4（端手互通 · 二维码同步）
- 📱💻 **真正的手机↔电脑互通**：设置页新增「二维码同步」——一台显示二维码（数据 gzip 压缩、
  多张自动循环播放），另一台扫一扫，训练记录/评估/预约/自定义动作几秒内自动合并到两台设备
- 🔒 无账号、无服务器、数据不出设备（仅在两台设备间通过二维码传输）；同 id 去重合并
- 🛠 模型加载加 jsDelivr 国内镜像 + 90 秒超时保护；摄像头画面立刻显示、模型后台加载（修黑屏）
- 🧪 新增 `?synctest=1` 同步自检（gzip 往返 / 二维码生成→像素→解码 / 合并去重）
### v2.3（视觉重设计 · 去模板感）
- 🎨 全新浅色「诊所手工风」主题：暖纸底 + 青绿主色 + 卡片阴影，告别深色 AI 模板
- 🎨 定制**火柴人品牌 Logo**：页面图标、按钮、导航、空状态全部换成手工线稿 SVG，
  彻底去除 emoji；深蹲/弓步/俯卧撑用**火柴人姿势简笔画**当图标，与画面骨架视觉统一
- 🎨 细节质感：按钮按压动效、周图表「今天」高亮、反馈面板图标气泡、
  带插图的空状态、loading 旋转图标、毛玻璃导航
- 🌍 中英双语与全部功能保持完好，9 项引擎自测通过
### v2.2（国际化 · 走向国际）
- 🌍 **全站中英双语**：标题/导航/训练反馈/评估报告/日程/设置全部双语，
  右上角一键切换（EN / 中文），自动检测浏览器语言并记住选择
- 🌍 英文品牌名 **Rehab AI**；日期/星期/导出文件名本地化（如 RehabAI-backup-日期.json）
- 🌍 动作引擎提示语（深蹲/弓步/俯卧撑/内扣/自定义规则）双语输出，数据标签双语显示
- 🌍 摄像头自检页 `camtest.html` 双语；`serve.py` 修复 Windows 中文输出崩溃
- 🏥 新增 favicon；`index.html?modeltest=1` 可自检 AI 模型加载（部署验证用）
- 🚀 **部署上线 GitHub Pages**（公开网址见 README 顶部）
### v2.1（性能与修复）
- 🐛 修复：保存训练记录时「不合格帧/深度」永远是 0 —— 现在每帧统计深度类别、
  不合格帧、内扣帧，记录页新增 **时长** 与 **内扣帧占比**
- 🐛 修复：预约列表把「今天还没到的时间」也标成已过期 —— 现在正确区分 今天/明天/已过期
- ⚡ 性能：统计数值每帧原地更新（不再每秒重建 DOM）；自定义动作列表加缓存
  （不再每帧读 localStorage）
- ⚡ 计数防抖：低于阈值需连续 120ms、两次计数间隔 ≥350ms，杜绝抖动重复计数
- 🔋 省电：切到其他页面或切到后台时暂停分析，回到训练页自动恢复
- 👻 火柴人只画可见关节（无幽灵线条）；画面没人时提示「未检测到人」
- 🎨 新增图标（favicon）；统计网格自适应不同动作的指标数量
