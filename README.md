# 康复AI · 火柴人姿势分析（Web 版 v2.2 · 中英双语）

手机 + 电脑通用的响应式 Web App：浏览器实时摄像头 → AI 姿态识别 → **火柴人骨架**
动作分析，集成记录打卡、自我评估、理疗预约、**自定义动作**、**数据采集训练闭环**。
视频不上传，AI 与数据全在本地运行。**中英双语，自动检测浏览器语言，一键切换。**

## 🌍 线上版（直接发给别人用）
**https://xushengqin666-cell.github.io/rehab-ai/**
（手机浏览器打开即可用摄像头；桌面二维码：`康复AI-线上网址二维码.png`）

## 支持的动作（火柴人实时分析 + 自动计数）
| 动作 | 分析指标 | 数据标签（采集用） |
|------|---------|-------------------|
| 🦵 深蹲 | 膝盖角 / 前倾 / 深度 / **膝盖内扣** | good/shallow/deep/lean/valgus |
| 🚶 弓步蹲 | 前膝角 / 后膝角 / 前倾 | good/frontShallow/frontDeep/lean |
| 💪 俯卧撑 | 肘角 / 身体直线度 | good/shallow/sag |
| ⭐ **自定义** | 自己定角度、合格范围、计数规则、提示语 | good/bad |

## 快速开始（本机）
```bash
python serve.py
```
浏览器打开 `http://127.0.0.1:8000`（电脑可开摄像头；手机同 Wi-Fi 可访问，摄像头需 HTTPS）

## 手机实时摄像头：部署上线（免费 HTTPS）
- **已部署**：https://xushengqin666-cell.github.io/rehab-ai/（GitHub Pages，推送即自动更新）
- **方式A（1分钟）**：打开 https://app.netlify.com/drop → 拖入本文件夹 → 得到网址，手机电脑同网址访问
- **方式B**：双击 `deploy.bat` → 按提示用 GitHub Desktop 发布到 GitHub Pages
- 跨设备数据互通：设置页「导出」→ 另一台「导入」

## 真实数据训练闭环（科研项目核心）
1. 训练页点「📝 数据采集」→ 选动作 → 摆姿势 → 点标签（每类 ≥50 条）
2. 设置页「导出采集 CSV」
3. 电脑上运行（Python 侧，rehab-ai 文件夹）：
   ```bash
   python train_from_web.py 康复AI采集数据-日期.csv
   ```
   → 按动作自动训练 RandomForest，输出交叉验证准确率 + `model_<动作>.pkl`

## 开发自测
浏览器打开 `index.html#selftest` → 控制台输出 9 项引擎自测（角度数学/内扣/计数含防抖/
弓步蹲/俯卧撑/自定义规则），全部 PASS 说明引擎正常。可用于 NCEA 测试证据。

## 目录结构
| 文件 | 说明 |
|------|------|
| index.html | 单页应用（训练/记录/评估/日程/设置） |
| style.css | 移动端优先响应式样式 |
| app.js | 主逻辑：火柴人绘制、采集、记录、评估、日程、自定义管理 |
| analysis.js | **动作引擎**：角度数学 + 深蹲/弓步蹲/俯卧撑/自定义分析 |
| i18n.js | **中英双语词典** + 语言切换 + 浏览器语言自动检测 |
| pose_landmarker_full.task | AI 姿态模型（本地，不上传） |
| vision_bundle.mjs + wasm/ | MediaPipe 浏览器运行时（本地，离线可用） |
| serve.py | 局域网启动器（打印手机访问地址） |
| deploy.bat | 一键部署准备（git 提交 + 上线说明） |
| netlify.toml | Netlify 部署配置 |

## 技术说明
- AI：MediaPipe PoseLandmarker（WebAssembly，本地运行）
- 判定阈值与 Python 版 rehab-ai 一致，可在 analysis.js 顶部调整
- 数据：浏览器 localStorage；导出 JSON 备份 / CSV 训练数据

## 更新日志
### v2.2（国际化 · 走向国际）
- 🌍 **全站中英双语**：标题/导航/训练反馈/评估报告/日程/设置全部双语，
  右上角一键切换（EN / 中文），自动检测浏览器语言并记住选择
- 🌍 英文品牌名 **Rehab AI**；日期/星期/导出文件名本地化（如 RehabAI-backup-日期.json）
- 🌍 动作引擎提示语（深蹲/弓步/俯卧撑/内扣/自定义规则）双语输出，数据标签双语显示
- 🌍 摄像头自检页 `camtest.html` 双语；`serve.py` 修复 Windows 中文输出崩溃
- 🏥 新增 favicon；`index.html?modeltest=1` 可自检 AI 模型加载（部署验证用）
- 🚀 **部署上线 GitHub Pages**（公开网址见 README 顶部）
### v2.1（性能与修复）
- 🐛 修复：保存训练记录时「不合格帧/深度」永远是 0 —— 现在每帧统计深度类别、
  不合格帧、内扣帧，记录页新增 **时长** 与 **内扣帧占比**
- 🐛 修复：预约列表把「今天还没到的时间」也标成已过期 —— 现在正确区分 今天/明天/已过期
- ⚡ 性能：统计数值每帧原地更新（不再每秒重建 DOM）；自定义动作列表加缓存
  （不再每帧读 localStorage）
- ⚡ 计数防抖：低于阈值需连续 120ms、两次计数间隔 ≥350ms，杜绝抖动重复计数
- 🔋 省电：切到其他页面或切到后台时暂停分析，回到训练页自动恢复
- 👻 火柴人只画可见关节（无幽灵线条）；画面没人时提示「未检测到人」
- 🎨 新增图标（favicon）；统计网格自适应不同动作的指标数量
